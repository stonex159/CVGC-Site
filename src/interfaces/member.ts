
export class Person {
    id: number;
    name: string;
    email: string;
    status: string;
  
    constructor(id: number, name: string, email: string, status: string) {
      this.id = id;
      this.name = name;
      this.email = email;
      this.status = status;
    }
  }